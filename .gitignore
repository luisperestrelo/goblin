# Never commit private keys (Enable Banking application key or any other)
*.pem
*.key
*.jks
*.keystore

# Signing config: points at the release keystore and holds its password
keystore.properties

# Gradle / Kotlin build state
.gradle/
.kotlin/
build/

# Android / IDE
local.properties
.idea/
captures/
*.apk
*.aab
output-metadata.json
